ERP_MYSQL_HOST=127.0.0.1
ERP_MYSQL_PORT=3307
ERP_MYSQL_DATABASE=lite_erp
ERP_MYSQL_USER=lite_erp
ERP_MYSQL_PASSWORD=LiteErp@2026!

# 外部/远程生产数据库必须使用显式非默认密码。也可只提供完整 URL，URL 中必须
# 包含强密码；无需再重复设置 ERP_MYSQL_PASSWORD。
# ERP_DATABASE_URL=mysql+pymysql://user:StrongPassword@db-host/lite_erp?charset=utf8mb4
# ERP_BUNDLED_LOCAL_MYSQL 仅由 run.py 为 127.0.0.1:3307 单机内置 MySQL 设置，
# 不能用于放行远程数据库的默认密码。

# JWT 签名密钥：生产环境（ERP_ENV=production）必须设置，否则后端拒绝启动。
# 生成方式：python -c "import secrets; print(secrets.token_hex(32))"
# ERP_JWT_SECRET=

# 运行模式（run.py 会自动设置，手工启动时请显式声明）：dev 允许临时随机密钥，
# production 强制要求 ERP_JWT_SECRET 和安全的外部数据库凭据；run.py 管理的
# 环回 bundled MySQL 使用受限内部模式保持一键单机启动兼容。
# ERP_ENV=dev

# Break-glass 应急管理员：默认关闭。三项必须同时设置，且密码不少于 12 位，
# 缺失或强度不足时应急账号一律不启用。每次应急登录都会写入独立审计日志。
# ERP_ENABLE_EMERGENCY_ADMIN=1
# ERP_EMERGENCY_ADMIN_USER=emergency_admin
# ERP_EMERGENCY_ADMIN_PASSWORD=
